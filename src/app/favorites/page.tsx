import type { Metadata } from "next";
import { PagePlaceholder } from "@/components/ui/page-placeholder";

export const metadata: Metadata = {
  title: "Favoris",
};

export default function FavoritesPage() {
  return <PagePlaceholder title="Vos favoris" />;
}
